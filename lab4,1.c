#include <stdio.h>

int main() {
    char alphabet;

    // Loop through each alphabet from 'a' to 'z'
    for (alphabet = 'a'; alphabet <= 'z'; alphabet++) {
        printf("%c ", alphabet);
    }

    printf("\n");

    return 0;
}
