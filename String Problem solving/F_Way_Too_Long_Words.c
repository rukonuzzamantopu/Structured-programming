#include <stdio.h>
#include <string.h>

int main() {
    int x;
    scanf("%d", &x);
    char a[100][1001];  

    for (int i = 0; i < x; i++) {
        scanf("%s", a[i]);  
    }

    for (int i = 0; i < x; i++) {
        if()
    }

    return 0;
}
